using UnityEngine;

public class E12_Script : MonoBehaviour
{
    int i;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int i;
        for (i = 0; i<=100; i++)
        {

            Debug.Log(i);
        }
         

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
